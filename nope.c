#include "monty.h"
/**
 * noOperation - Does nothing; a placeholder function.
 * @stack_head: Pointer to the stack's head
 * @line_number: The line number where this operation is encountered
 * Return: This function does not have a return value.
 */
void f_nope(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
